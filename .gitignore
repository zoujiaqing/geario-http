target/
Cargo.lock
geario-http-ffi/examples/version

# Cross-compilation linkers are a property of the machine, not the project:
# naming a linker for the host target here breaks a native build on it.
.cargo/
