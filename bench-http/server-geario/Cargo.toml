[package]
name = "server-geario"
version = "0.1.0"
edition = "2024"

[features]
uring = ["geario/neon-uring"]
polling = ["geario/neon-polling"]

[dependencies]
geario = { path = "../../../geario/geario" }
geario-http = { path = "../../geario-http", features = ["http1", "server"] }
